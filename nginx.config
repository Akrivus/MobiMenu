server {
  listen 80 default_server;
  listen [::]:80 default_server;

  server_name hammerhand.local;
  client_max_body_size 500M;

  location / {
    proxy_pass  http://localhost:4567;
    proxy_set_header Host  $host;
    proxy_set_header X-Real-IP $remote_addr;
  }
}